##### [Rocket.Chat](https://rocket.chat/docs/installation/docker-containers/docker-compose) image:

```bash
docker-compose up -d mongo
docker-compose up -d mongo-init-replica
docker-compose up -d
Stayin' Alive
```
